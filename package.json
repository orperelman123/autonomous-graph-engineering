{
  "name": "autonomous-graph-engineering",
  "version": "0.1.0",
  "private": true,
  "description": "Permission-safe prompt refinement and autonomous graph orchestration for Codex and Claude Code.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @autonomous-graph-engineering/prompt-refiner && npm run build --workspace @autonomous-graph-engineering/graph-engineer",
    "typecheck": "npm run build --workspace @autonomous-graph-engineering/prompt-refiner && npm run typecheck --workspace @autonomous-graph-engineering/prompt-refiner && npm run typecheck --workspace @autonomous-graph-engineering/graph-engineer",
    "pretest": "npm run build",
    "test": "npm run test --workspaces --if-present && tsx --test test/schema-contracts.test.ts test/launch-readiness.test.ts",
    "preeval": "npm run build --workspace @autonomous-graph-engineering/prompt-refiner",
    "eval": "npm run eval --workspaces --if-present",
    "check": "npm run typecheck && npm test && npm run eval && npm run build",
    "install:local": "node scripts/install.mjs",
    "verify:install": "node scripts/verify-install.mjs",
    "predoctor": "npm run build",
    "doctor": "node scripts/doctor.mjs",
    "demo": "node scripts/demo.mjs",
    "prebenchmark": "npm run build",
    "benchmark": "node scripts/benchmark.mjs",
    "secret-scan": "node scripts/secret-scan.mjs",
    "link-check": "node scripts/check-links.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
